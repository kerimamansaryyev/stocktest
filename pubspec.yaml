name: stocktest
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  adaptix: ^0.0.5
  cupertino_icons: ^1.0.2
  characters: ^1.2.1
  fl_chart: ^0.55.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^0.13.5
  intl: ^0.17.0
  manager_provider: ^0.4.2
  meta: ^1.8.0
  provider: ^6.0.4
  
dev_dependencies:
  dart_code_metrics: ^4.17.1
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  

flutter:
  uses-material-design: true
  generate: true
  